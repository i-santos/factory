# Expose Run State Through GUI And API

## Source Work Package

`.factory/02-yard/a-refining/20260512T135038Z-autonomous-visual-runs.dir`

## Requested Outcome

The local GUI exposes durable run records and renders minimal human decision panels.

## Scope

- Add `/api/runs`.
- Load runs for the root GUI page.
- Add run monitor markup.
- Render `needs-human-action` state.
- Render basic controls for option lists, text, textarea, checkbox, button, and form-like inputs.

## Completion Signal

The GUI/API can show run state and decision request data without requiring Markdown file inspection.
